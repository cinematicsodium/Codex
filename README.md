# Codex Skills

Repo-scoped Codex skills live in `.agents/skills/`.

Current skills:

- `nothing-design`: Nothing-inspired UI/UX design system.
- `oop-refactoring`: Practical OOP refactoring while preserving behavior.

Codex reads `AGENTS.md` automatically. Codex discovers skills from `.agents/skills`, then reads a skill's full `SKILL.md` only when that skill is selected.
